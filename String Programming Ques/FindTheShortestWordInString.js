//!27. Find the shortest word in a string.
// Input: `"I am learning JavaScript"`
// Output: `"I"` 

let str="I am learning JavaScript"
let currentWOrd='';
function FindShortestWordInString(){
    for(let i=0;i<str.length;i++){
        if(str[i]== ' ' || i===str.length-1)
    }
}